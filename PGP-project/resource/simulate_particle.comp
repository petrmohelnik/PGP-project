#version 450

layout(local_size_x = 64, local_size_y = 1, local_size_z = 1) in;

layout(std430, binding = 0) buffer particleBuffer{
    vec3 particle[];
};

void main()
{
    uint id = gl_GlobalInvocationID.x;
    particle[id] = particle[id] + vec3(0.01, 0.01, 0.01);
}